# Keep false unless the deployment is authorized to send case context to an external LLM.
ENABLE_EXTERNAL_LLM=false

# Configure one provider only when external LLM use is approved.
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
# OPENROUTER_API_KEY=
# OPENROUTER_MODEL=openai/gpt-4o-mini
